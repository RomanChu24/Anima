meta:
  environment: nodejs
  toolchain:
    name: node
    version: 20
build:
  skip: false
  additionalCommands: npm run build
run:
  command: npx next start -p 80
  persistenceMount: /data
  containerPort: 80
