meta:
  environment: nodejs
  toolchain:
    name: node
    version: 20

build:
  buildDir: .
  buildCmd: rm -rf next-dist && npm ci && npm run build

run:
  persistenceMount: /data
  runCmd: npx next start -p 80
